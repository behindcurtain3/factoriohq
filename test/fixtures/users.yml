# Passwords are bcrypt-digested via Devise so the records are usable in tests.
one:
  email: one@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, "password123") %>
  admin: true
  factorio_username: player_one
  factorio_token: secret-token

two:
  email: two@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, "password123") %>
  admin: false
